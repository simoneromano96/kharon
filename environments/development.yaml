# Database
database:
  # Connection URI
  uri: postgres://kharon:kharon@localhost:5432/kharon
  # Database name
  name: kharon
# Casbin access model
casbin:
  accessmodelpath: ./environments/access_model/rbac_model.conf
# HTTP Server
app:
  port: 8000
